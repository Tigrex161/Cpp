#include <iostream>
#include <thread>
#include <string>
using namespace std;

void add(int x, int z = 0)
{

    int y(0);
    i = z;
    for(i; i <= x ; ++i)
    {

        y += i;
        cout <<  y  << endl;
    }

cout <<  y  << endl;
}


int main()
{
thread t1(add, 500);
thread t2(add, 1000, )
/*unsigned int nthreads = std::thread::hardware_concurrency();
cout << nthreads;*/
int a;
cout << "Enter a number ";
cin >> a;

cout << a;

t1.join();

    return 0;
}

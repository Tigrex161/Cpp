#include <iostream>
using namespace std;
extern int divisor;
int compare(int, int);
int square(int);
long hmstosec(int,int,int);
int isPerfect(int);

int main()
{
    /*int a;
    cout << "Enter the first number " << endl;
    cin >> a;
    int b;
    cout << "Enter second number " << endl;
    cin >> b;

    compare(a,b);*/

  /*  cout << "Enter the desired length of the side of the square ";
    int side;
    cin >> side;
    square(side);
*/
    /*int h,m,s;
    char a(':');
    cout << "Enter the time as HH:MM:SS ";
    cin >> h >> a >> m >> a >> s;
    hmstosec(h,m,s);*/

    /*int p;
    cout << "Enter a number ";
    cin >> p;
    isPerfect(p);*/

for (int x(1); x <= 100000 ; ++x)
{
          isPerfect(x);

}


return 0;
}



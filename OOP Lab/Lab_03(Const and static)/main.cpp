#include <iostream>
#include "tollBooth.h"
#include "time.h"
#include "serial.h"
#include "polynomial.h"
using namespace std;

int main()
{
       /*tollBooth one;
        int x;
        char y;

        while(true)
        {
            cout << "Enter a key: ";
            cin >> y;
            x = y;

            if(x == 112 || x == 80)
            {
             one.payingCar();
            }

            if(x == 110 || x == 78)
            {
            one.noPayCar();
            }

            if(x == 27)
            {
                one.display();
                break;
            }

        }*/


        /*time1 a(11,20,35);
        time1 b(11,20,35);
        time1 c;
        c = a.add(b);
        c.display();*/

    /*serial one;
    serial two;
    serial three;
    one.getID();
    two.getID();
    three.getID();*/

    /*
    polynomial one;
    one.setCoefficients();
    one.getDegree();
    polynomial two;
    two.setCoefficients();
    polynomial three;
    three = one.multiply(two);
    three.display();
    one.evaluate(2);
    */

    return 0;
}
